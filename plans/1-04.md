# V2.0 Plan

## standardize routes structure and navigation
Can you refractor the routes? perhaps generate a new object call Route which contain the detailRoutes, InfoRoutes etc, and a couple of routes not alread defined as constants. Also For acivities, that has so many pass ins, can you adapt navhost or navigator instead of creating inline function just to nav.navigate?


## Standardize String
For all strings that eventually propagates to the app UI, and string that needs to be displayed on the UI, can you please factor them out to String resources so they can be work with internationalization? also create a string resources for the Chinese Simplified, and work out the language setting so that I can test out system default to English to Chinese language switch

##
All done 